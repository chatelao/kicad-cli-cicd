name: PlatformIO CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Install xmllint
        run: |
          sudo add-apt-repository ppa:kicad/kicad-dev-nightly
          sudo apt update
          
      - name: Install KiCAD (nightl)
        run: sudo apt kicad-nightly
        
      - name: Get KiCad version
        run: kicad-cli version

